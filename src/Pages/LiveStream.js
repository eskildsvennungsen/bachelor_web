import React from 'react';
import LiveStream from '../Component/LiveStreamComponent';

const LiveStreamPage = () => {
    return (
        <React.Fragment>
        <LiveStream />
        </React.Fragment>
    );
};

export default LiveStreamPage;
